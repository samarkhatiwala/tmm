This model requires the following input files:

% surface forcing
atmosp.bin or atmosp_**
fice.bin or fice_**
wind.bin or wind_**
OR (optionally and if periodic forcing)
if separate winds
uwind_**
vwind_**

% full 3-d fields
Ss.petsc or Ss_**
Ts.petsc or Ts_**

% initial conditions
trini.petsc
